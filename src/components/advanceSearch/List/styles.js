import styled, { css } from 'styled-components';

const StyledForm = styled.div(() => {
  return css`
    padding: 1rem;
    .SearchTabs span {
      color: black;
      font-size: larger;
    }
  `;
});

export default StyledForm;
