import styled, { css } from 'styled-components';

const StyledForm = styled.div(() => {
  return css`
    .SearchTabs span {
      color: black;
      font-size: larger;
    }
  `;
});

export default StyledForm;
