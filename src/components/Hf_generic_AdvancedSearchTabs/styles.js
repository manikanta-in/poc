import styled, { css } from 'styled-components';

export default styled.div(() => {
  return css`
    margin: 0px 0;
    background-color: white;

    .SearchTabs span {
      color: black;
      font-size: larger;
    }
  `;
});
